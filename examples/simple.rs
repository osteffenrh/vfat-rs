use vfat_rs::VfatFS;

fn main() {
    //
    /*
       let file = File::new("/tmp/vfat.fs");
       let vfat = Vfat::new(file);
       let root = vfat.get_root();
       let tmp = root.create_dir("/tmp");
       tmp.create_file("file.txt");
       tmp.delete_file("file.txt");
       root.delete_dir("/tmp");


    */
}
